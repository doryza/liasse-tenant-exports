# Tenant API variables for local testing.
# Tenant: ristorantevoga (Ristorante Voga)
# Copy to .env.local in this directory and fill in real values.
# .env.local is gitignored — never commit real keys.
#
# This site has no required API variables. The reservation widget is pasted
# into the admin backend (Réservations) as raw embed code / link, not an env var.
