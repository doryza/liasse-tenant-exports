const makePayPal=require('./lib/paypal');
module.exports=function(services){const paypal=makePayPal(services);services.scheduler.register('sensoreel-payment-reconciliation',300000,async({db})=>{try{const raw=Object.fromEntries((await db.all('SELECT key,value FROM admin_settings')).map(x=>[x.key,x.value]));if(!paypal.enabled(raw,!!(services.config.isPreview||services.config.preview)))return;const rows=await db.all("SELECT * FROM appointments WHERE paypal_order_id IS NOT NULL AND payment_status NOT IN ('refunded','denied') AND created_at>NOW()-INTERVAL '180 days' ORDER BY checked_at ASC NULLS FIRST LIMIT 10");for(const row of rows){try{await paypal.sync(row);}catch(e){await db.run('UPDATE appointments SET checked_at=NOW() WHERE id=$1',[row.id]);}}}catch(e){}});};